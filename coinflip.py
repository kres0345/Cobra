import time, random
chance = (random.randint(1,2))
if chance == 1:
    print "head"
if chance == 2:
    print "tail"
time.sleep(5)
# I created this program while I had forgotton that CobraConsole is a bat
# and not a python file.
